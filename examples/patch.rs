use essay_graphics::{artist::Patch, layout::Figure, prelude::*};

fn main() { 
    /*
    let mut figure = Figure::new();
    let mut graph = figure.new_frame(());

    let patch = Patch::new(Path::move_to(0., 0.)
        .line_to(1., 0.)
        .close_poly(1., 1.)
    );

    graph.artist(patch);

    figure.show();
    */
}
